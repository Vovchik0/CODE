# Зависимости Minecraft Load Tester
#
# Графический интерфейс построен на PyQt4. PyQt4 не устанавливается через pip
# для большинства современных окружений и не поддерживается официально с 2018 г.
# Установите его средствами системы:
#
#   Debian/Ubuntu :  sudo apt-get install python3-pyqt4
#   Fedora        :  sudo dnf install PyQt4
#   macOS (brew)  :  brew install pyqt@4
#   Windows       :  используйте неофициальные wheels PyQt4 под вашу версию Python
#
# Ниже указана строка для pip на случай, если в вашем окружении доступен
# соответствующий пакет/wheel. Если установка через pip не удаётся -- используйте
# системный менеджер пакетов (см. выше).
PyQt4

# Остальная функциональность использует только стандартную библиотеку Python
# (socket, threading, queue, json, csv, uuid, logging), дополнительные пакеты
# не требуются.
